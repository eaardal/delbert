<UserControl x:Class="Delbert.Components.SelectRootDirectoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Delbert.Components"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
            <ColumnDefinition Width="Auto" MinWidth="250"></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="Select root directory" FontSize="14" FontWeight="SemiBold"></TextBlock>
        <TextBox Grid.Column="1" Text="{Binding Path=RootDirectoryPath}"></TextBox>
        <Button Grid.Column="2" x:Name="BrowseRootDirectory" Content="Browse" Margin="3" Padding="3"></Button>
    </Grid>
</UserControl>
